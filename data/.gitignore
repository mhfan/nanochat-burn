# Ignore generated datasets and caches
*.jsonl
# Keep deterministic, repository-owned smoke-test fixtures under version control.
!fixtures/tiny/*.jsonl
eval/
downloads/
# Materialized by pretraining from the configured corpus and tokenizer.
pretrain.bin
pretrain.txt
